swagger: "2.0"
info: 
  version: "0.0.1"
  title: "cpuinfo"
  description: "A simple service to get Key Value Store using swagger-2.0 specification and codegen"
  termsOfService: "http://swagger.io/terms/"
  contact: 
    name: "Firestore Service Example"
  license: 
    name: "Apache"
host: "localhost:8080"
basePath: "/api"
schemes: 
  - "http"
consumes: 
  - "application/json"
produces: 
  - "application/json"
paths: 
  /keyvalstore:
    get: 
      description: "Returns key value store object"
      produces: 
        - "application/json"
      responses: 
        "200":
          description: "Key Value Store"
          schema: 
            $ref: "#/definitions/User"
definitions:
  User:
    type: "object"
    required: 
      - "model"
    properties: 
      model:
        type: "string"
